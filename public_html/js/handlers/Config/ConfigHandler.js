export default function ConfigHandler(props){
	/* props: {server:{serverURL}, RENDER, DARK_THEME} */

	this.server = props.server

	// TODO:
	this.RENDER = props.RENDER;
	this.DARK_THEME = props.DARK_THEME;
}
